<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pj.third.se.repository.UserMemberRepository">
    <insert id="saveUser"  parameterType="pj.third.se.Vo.UserMemberVo">
        INSERT INTO tbl_user(u_id, u_pw, u_name, u_gender, u_mail, u_phone, u_birth, u_reg_date, u_mod_date)
        VALUES (#{u_id}, #{u_pw}, #{u_name}, #{u_gender}, #{u_mail}, #{u_phone}, #{u_birth}, NOW(), NOW())
    </insert>

    <select id="selectMyInfo" parameterType="pj.third.se.Vo.UserMemberVo">
        SELECT * FROM tbl_user WHERE u_no =#{u_no}
    </select>


</mapper>
